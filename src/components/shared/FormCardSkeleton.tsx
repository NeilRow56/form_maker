import React from 'react'
import { Skeleton } from '../ui/skeleton'

type Props = {}

const FormCardSkeleton = (props: Props) => {
  return <Skeleton className="border-primary-/20 h-[190px] w-full border-2" />
}

export default FormCardSkeleton
